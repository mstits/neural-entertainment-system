# Core emulation
# nes_core is the in-tree Rust NES core, built via maturin (see
# nes_core/pyproject.toml). No PyPI dep needed — the wheel installs
# into .venv from the local crate. Python-side helpers live here.

# Neural network + training
# torch is installed separately in install_macos.sh to ensure MPS support
numpy>=1.24,<2.0
scipy>=1.11,<2.0

# GUI
PyQt6>=6.6.0,<7
pyqtgraph>=0.13.3,<1

# Config and utilities
PyYAML>=6.0,<7
click>=8.1.0,<9
rich>=13.7.0,<16

# Metrics and visualization
matplotlib>=3.8.0,<4
tensorboard>=2.15.0,<3
# Pillow is imported by name (`from PIL import Image`) by six files under
# tests/parity and by nine under scripts/. It was never declared here and only
# ever arrived as a transitive dependency of matplotlib, so the parity
# tooling breaks the day matplotlib stops pulling it. Declared directly.
pillow>=10.0.0,<14

# Checkpointing and serialization
dill>=0.3.7,<1

# Dev tooling
pytest>=7.4.0,<10
pytest-cov>=4.1.0,<8
# pytest-timeout backs the `--timeout` used by every `make test*` target;
# pytest-qt backs the GUI UAT tests. Without these a fresh clone's first
# quality signal (`make test`) errors on an unrecognized argument.
pytest-timeout>=2.1.0,<3
pytest-qt>=4.2.0,<5
# hypothesis backs the property tests in tests/test_properties.py. Missing
# here until 2026-09-02, which made `pytest tests/` on a clean clone exit 2
# at collection with ModuleNotFoundError and run zero tests, while the same
# command stayed green on any machine that happened to have it installed.
hypothesis>=6.100,<7
ruff>=0.1.9,<1
mypy>=1.7.0,<2
