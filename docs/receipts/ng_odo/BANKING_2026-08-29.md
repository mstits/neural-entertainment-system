# Odometer-probe family: banked receipts (the archival-unblock pass)

**Date:** 2026-08-29. **Operator-approved** (the "banking pass first,
then archive" path). Until today the raw run directories under
`runs/{ng,rygar}_odo_*` (~206G) were the ONLY holders of figures cited
by `docs/research/RECEIPTS_INDEX_2026-08-24.md` and
`docs/research/CAPABILITY_REPORT_2026-08-24.md` — vetoed from archival
under the citation rule. This directory banks the evidence-bearing
small files from all 15 dirs (per dir: `archive.stats.json`,
`progress.jsonl`, `roots.json`, a `solutions_listing.json`), so the
raw dirs become archivable; the multi-GB `archive.pkl`/`traces.pkl`
survive only inside the archival `.tar.zst` files (the deep
re-derivation path, one-command restore).

**Every cited figure was re-derived from the BANKED copies before this
bank was trusted** (per the promoted cross-derivation invariant;
receipt: `rederivation_2026-08-29.json`, which also carries sha256 +
size for every banked file):

| Cited figure | Citing doc | Re-derived from bank |
|---|---|---|
| ng_odo_scene: max_area 8, 8 scenes in 12 min | RECEIPTS_INDEX §scene-detection | max_area 8 at elapsed 721 s ✓ |
| ng_odo_night: 3 h, 17.7 M steps, best 6144 | RECEIPTS_INDEX pre-scene plateaus; CAPABILITY_REPORT | 10,803 s, steps 17,758,970, gx 6144 ✓ |
| scene_long/long2: 4 h each; max_area 9, max_sect 7, best_score 74,783 | both docs | 14,411/14,414 s; 9 / 7 / 74,783 ✓ |
| throttle, doa: shorter follow-ups, same frontier | both docs | 1,584 s / 1,208 s; 9 / 7 / 74,783 ✓ |
| ng_odo_doa = FORGE dead-on-arrival retirement receipt run (6a47a71) | RECEIPTS_INDEX | stats + full progress banked |
| rygar_odo_night: 3 h, 21.5 M steps, 5,680 px | both docs | 10,800 s, steps 21,529,240, gx 5680 ✓ |
| rygar_odo_debounce: "gx 1536 → 5360 in 6 min" | RECEIPTS_INDEX | gx 5360 at 360 s ✓ — **the 1536 pre-fix baseline is NOT in this dir's rows** (its first row, 60 s in, already reads 3263); 1536 is the pre-debounce run's plateau, sourced outside this dir. Banked as-is, not overclaimed. |
| "0 solutions" (all runs) | both docs | `solutions: 0` in every final row + empty `solutions/` listings ✓ — **read under the 2026-08-26 CORRECTION**: all 15 profiles shipped `level_key: []`, making `is_clear` the `() > ()` identity, so this figure means "the question was never asked," NEVER "unsolved." RECEIPTS_INDEX carries the correction block verbatim. |

`hw_provenance` (nes_core sha256_16, frame_skip, hw flags) and
`key_config` ride inside each banked `archive.stats.json`.
