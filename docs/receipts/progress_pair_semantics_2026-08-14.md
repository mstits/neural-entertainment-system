# Progress-pair semantics classifier — 2026-08-14

Generated by `scripts/classify_progress_pairs.py`. No emulator was driven to produce this table — every verdict traces to an already-banked discovery/verification receipt under `docs/receipts/`; see each row's Source. The rule: a low-byte wrap coinciding with a +-1 high-byte change within <=1 sample on **>90%** of observed wraps => `carry-coupled`; hi flat across wraps on **>90%** => `composite`; anything short of either bar, or no receipted wrap count at all, => `UNKNOWN` (never guessed).

| game | verdict | wraps (coupled/total) | evidence source |
|---|---|---|---|
| castlevania | `carry-coupled` | 8/9 | `docs/receipts/ram_verify/castlevania.json` |
| contra | `carry-coupled` | 7/7 | `docs/receipts/ram_verify/contra.json` |
| ghosts_n_goblins | `carry-coupled` | 28/28 | `docs/receipts/games/ghosts_n_goblins_receipt.json` |
| kirby | `UNKNOWN` | 174/197 | `docs/receipts/kirby/kirby_observables_receipt.json` |
| double_dragon | `UNKNOWN` | 2/3 | `docs/receipts/games/double_dragon_receipt.json` |
| kid_icarus | `composite` | — | `docs/receipts/games/kid_icarus_receipt.json` |
| excitebike | `composite` | — | `docs/receipts/games/excitebike_receipt.json` |
| gradius | `UNKNOWN` | — | `docs/receipts/games/gradius_receipt.json` |
| metroid | `UNKNOWN` | — | `docs/receipts/games/metroid_receipt.json` |
| megaman | `UNKNOWN` | — | `docs/receipts/megaman/mm2_observables_receipt.json` |

## Evidence

### castlevania (`configs/castlevania.yaml`, lo=0x0040 hi=0x0041)

probe alone: 8/9 = 88.9% (rule verdict on this count alone: UNKNOWN, below the 90% bar on n=9); combined with the mechanistically verified gx-767 tear documented in scripts/go_explore_solve.py (the borrow $0040/$0041 carry this repo's torn-read filter exists to catch), the honest call is carry-coupled. configs/castlevania.yaml is READ-ONLY (user WIP) — recommended block below, not applied.

### contra (`configs/contra.yaml`, lo=0x0065 hi=0x0064)

7/7 wraps coupled (100%). Re-verified 2026-08-10 on a fresh core build: 3/3 wrap-coupled (docs/receipts/games/contra_reentry_2026-08-10.md), same pair.

### ghosts_n_goblins (`configs/ghosts_n_goblins.yaml`, lo=0x006C hi=0x0065)

28/28 wraps coupled (100%), the cleanest of several candidate high bytes tested (28/33 and 21-29/33 for the runners-up). NOTE: this CONTRADICTS go_explore_solve.py's borrow_followup docstring, which lists ghosts_n_goblins among the six '(screen, x) composites' on an address-adjacency assumption (0x0065/0x006C are 7 bytes apart) — adjacency turns out not to predict carry behaviour here; the receipted count does. The runtime self-audit (BORROW_REFUTE_LIMIT) would have caught this in production had the flag ever been turned on; this is why it stayed off.

### kirby (`configs/kirby.yaml`, lo=0x0083 hi=0x0095)

174/197 = 88.3% combined (down 128/151 = 84.8%, up 46/46 = 100.0%). Below the 90% bar despite n=197 — the down-direction shortfall (84.8% vs up's 100%) reads like the frame_skip aliasing double_dragon's receipt independently diagnosed (a multi-wrap step collapsed into one sample), not randomness, but this script does not adjust receipted counts for a hypothesis it cannot verify. UNKNOWN stands until a dense (frame_skip=1) recount settles the down direction.

### double_dragon (`configs/double_dragon.yaml`, lo=0x005A hi=0x00B2)

2/3 events clean +1 — n=3 is far too small to clear or miss a 90% bar with any confidence; UNKNOWN on insufficient data, not on the ratio itself.

### kid_icarus (`configs/kid_icarus.yaml`, lo=0x0750 hi=0x04D1)

structural, not a wrap count: the receipt's own honesty_note — '0x0750 and 0x04D1 are NOT frame-locked (0x0750 does not wrap 255->0 exactly at each screen boundary — a screen is ~108-256 fine units, not exactly 256). This is a (screen<<8 | fine) COMPOSITE that is empirically monotone in continuous play (mono 1.000), not a textbook lockstep 16-bit pair. A small backward seam artifact of <=255 can occur at a boundary frame; Go-Explore domination absorbs it (verified: the solver climbed cleanly to 2040).' — states the low byte never reaches the 0xFF/0x00 boundary (a screen spans ~108-256 fine units), so there is no wrap to test co-occurrence on; composite by construction.

### excitebike (`configs/excitebike.yaml`, lo=0x00ED hi=0x03A4)

structural: 'The monotone lo|hi encoding is section-based (section_dist 0..129 never reaches 255, so it never wraps; the section_index increments cleanly at each boundary). A search for a TRUE 16-bit world-X odometer (a low byte wrapping 255->0 with a hi byte +1 at each wrap) returned ZERO valid lo/hi pairs across 336 wrapping low-byte candidates -> Excitebike has no global position odometer; the section+dist counter IS the world progress (player sprite is camera-fixed, the track scrolls).' — an exhaustive search for a carry pair anywhere in RAM found none; the profile's lo (section_dist) never reaches 255 (capped ~129), so no wrap event exists at all.

### gradius (`configs/gradius.yaml`, lo=0x003E hi=0x003F)

no receipted wrap/couple count exists. Qualitative evidence leans carry-coupled — adjacent addresses, and described as a single '16-bit little-endian forced-scroll accumulator' ('nondec=1.000, zero backward jumps over every scripted first life: 11->836 (deep suicide life), 11->429 (shallow noop life), 11->161 (fwd life).') that stays monotone across several 256-multiple boundary crossings — but this script only asserts a verdict off an actual wrap count, and none is banked. Candidate for a targeted recount, not a guess.

### metroid (`configs/metroid.yaml`, lo=0x0051 hi=0x0050)

no receipted wrap/couple count exists — only combined-signal monotonicity ('flat under NOOP; rises hold-right (768->1500), falls hold-left (->~512); combined signal 99% monotone non-decreasing under hold-right (prog_monotone_nondec_frac 0.99)'). Adjacent addresses (0x0050/0x0051) are suggestive but this script does not verdict off adjacency.

### megaman (`configs/megaman.yaml`, lo=0x0460 hi=0x0440)

no N/M wrap count is banked, only a pass/fail + one worked example: 'PASS. 0x0460 wraps 255->63 EXACTLY when 0x0440 steps 0->1 (a real screen advance); the low byte never wraps without the high byte incrementing. This is a position odometer, not a clock.' The 7-checkpoint cum-displacement match ('PASS. Trajectory-anchored depth ladder of 7 states at independently-measured cumulative displacement [63,127,191,255,319,383,424] px. The odometer (0x0440<<8 | 0x0460) reads [191,255,319,383,447,511,552] -- strictly monotone AND exactly equals cum+128 (a constant 128-px start offset) at every rung, byte-for-byte. So the RAM odometer IS the true absolute X.') is consistent with a true odometer, not a screen-reset composite. NOTE: this also CONTRADICTS go_explore_solve.py's borrow_followup docstring, which lists megaman among the '(screen, x) composites' on the same address-adjacency assumption ghosts_n_goblins' receipt refutes. UNKNOWN here reflects the missing count, not doubt that the qualitative evidence points the other way — a dense recount is the next step, not this script overriding the docstring on prose alone.

## Configs edited

- contra: not applied (no bare lo/hi match, or already set)
- ghosts_n_goblins: not applied (no bare lo/hi match, or already set)

## castlevania — recommended block (config is READ-ONLY, not applied)

```yaml
  progress: {lo: 0x0040, hi: 0x0041, smooth: hampel}
```

hampel over median3: median3 drops every sample of a genuine sustained jump (a warp / room load) indefinitely — it compares each new sample only to the two immediately before it, so once the trajectory has moved on, the window has too, and it never recognises the new level as real. hampel's persistence check (scripts/go_explore_solve.py, progress_glitch's `hampel` branch) admits a sustained jump within one sample of delay instead, which matters for a hall game with real level/block transitions.

## Open follow-ups

- kirby, double_dragon: receipted counts exist but land in UNKNOWN (kirby just under the 90% bar on n=197; double_dragon's n=3 is too small either way). A dense frame_skip=1 recount would settle both.
- gradius, metroid, megaman: no receipted wrap/couple count at all — only qualitative monotonicity evidence. megaman's qualitative signal contradicts go_explore_solve.py's borrow_followup docstring (which assumed non-adjacent lo/hi addresses imply no carry); so does ghosts_n_goblins' receipted 28/28. Address adjacency is not a reliable proxy for carry behaviour — only a receipted wrap count is.

