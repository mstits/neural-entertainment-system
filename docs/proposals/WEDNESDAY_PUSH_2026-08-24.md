# THE WEDNESDAY PUSH — master orchestration prompt

Authored 2026-08-24 evening. Directive: consume the week's remaining
Claude utilization by Wednesday morning on maximum real advancement.
Doctrine unchanged: purity line, honest eval, pre-registered gates,
receipts for every claim, VOID ≠ FAIL. Token spend is the fuel, not
the deliverable — every workflow below ends in a committed artifact.

## The five lanes (each a repeating workflow family)

**Lane A — League onboarding at scale.** The library boots 793 ROMs;
a mechanical onboarding recipe exists (start-state mint → observable
discovery by experiment → odometer/progress gate → profile → 10-min
solver smoke → classification). Fan out one agent per game, waves of
8–14. Deliverable per game: a committed profile + gate receipt +
classification row (SOUND-advancing / camera-static / skill-wall /
needs-scene-keys / needs-mechanism). Deliverable per wave: the League
classification table grows. This is the totality-basis direction made
concrete.

**Lane B — Room-graph engine (the capability directive).** The parts
are shipped and validated separately: scene ordinals, per-scene
odometer, peek_nametables fingerprints, door-transition macros.
Assemble them into room-graph navigation for Zelda/Metroid-class
games: room identity by masked nametable hash, room-graph archive
keys, item/key event discovery via irreversible-bit probes (v23), and
route search over the graph. Workflow phases: understand (parallel
readers) → design (judge panel of 3 independent designs) → implement
(worktree agents) → verify (tests + live Zelda D1 / Metroid shaft
probes). Purity: hardware surfaces and experiment-discovered bits
only.

**Lane C — v27 training science (local compute, agent-adjudicated).**
The registered question: does putting the 27 mined recovery states in
the curriculum FROM THE START of a fresh run beat the banked 0.767
(post-hoc training is closed — every family failed on the consolidated
artifact)? One agent designs the config + preregistration addendum;
training runs on the M4 between workflows; agents adjudicate
checkpoints honestly. Secondary: v27 DR prompt authored and fired with
the isolated-optimum autopsy.

**Lane D — World-class hardening waves.** The proven audit shape:
dimensions → find → adversarially verify (2-of-3 refuters) → fix →
test-verify. Targets: the two pre-existing parity failures (tecmobowl
golden, Contra lockstep ceiling — root-cause, don't paper over), the
Rust core (odometer/scene/serialize additions are young), the solver
(today's five new mechanisms), and the training stack. Every
confirmed finding becomes a fix commit with a test.

**Lane E — Claims ledger + capability synthesis.** Agents sweep the
receipts corpus (runs/, docs/research/, docs/proposals/, memory) and
produce: updated CLAIMS.md entries with receipt paths, a capability
report (what the system can do today vs two weeks ago, with numbers),
and a curated receipts index. The "walk away and see a dramatically
improved piece of software" surface.

## Sequencing

Waves launch continuously; workflow completions chain the next wave.
A/D/E are independent — run concurrently. B is phased — understand+
design first, implement after review. C's training holds the M4
between eval bursts. Repeat waves until Wednesday morning or the
budget is consumed, whichever first. Every wave ends with commits;
nothing lands without its receipt.
