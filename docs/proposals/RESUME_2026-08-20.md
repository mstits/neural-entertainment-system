# RESUME — 2026-08-20, engine left running unattended

## Running now

The engine is under launchd (`com.stits.nes-engine`), ticking every 10
minutes, surviving reboot. Stop it with

    launchctl unload ~/Library/LaunchAgents/com.stits.nes-engine.plist

Queue, in order:

1. **`hazard_collect_full`** — 104,640 causal micro-fork labels from the
   871-step 1-2 tape. ~45 min.
2. **`hazard_phase2_train`** — the discrete-time survival model.
   Pre-registered gate: **Uno IPCW C-index >= 0.85**, split by source
   state. Below it, the synthesis says the 13x13 tile observation lacks
   the resolution to see threats and the substrate is not integrated.
   This is a real research verdict either way.
3. **`replay_sweep_full`** — all 298 banked tapes replayed from their own
   roots against their own profiles' `is_clear`.

## The important thing that happened today

**The hazard Phase-1 KILL was false.** The engine benchmarked twelve
minutes after a thirteen-hour campaign and measured 100.1 steps/s against
a 1,000 threshold — a KILL on the direction three independent Deep
Research rounds converged on. On a quiet machine the identical command
measures **2,283 steps/s and PASSES** at 44.5 min against a 60-min
budget.

Falsified before accepting it: pure `step_all` runs at 2,267
worker-steps/s, the same workload with a savestate load per batch at
2,509, and loads alone at 635k/s. **Micro-forking costs nothing.** The
gate had measured the machine, not the method.

Benchmarks now declare `needs_quiet` and defer until load < 3.0 and 15
minutes after the last emulator job. The brief already required this;
nothing enforced it.

## Results banked today

- **2-1: 0/100 honest**, both seeds, checkpoint iter_01040. Not banked.
  It passed its deterministic rung gate 10/10 at median 3299 and still
  clears zero times cold. That gap is now measured on a fifth level.
- **1-4 re-consolidation finished at 0.633 strict** on its final
  30-episode probe (median 2429) against a banked 51%. NOT a claim —
  winner's curse — but the first evidence re-consolidation can raise a
  level rather than collapse it, which is the opposite of 2-1.
- **1-3 endpoint: 0.0** on 50 episodes.

## Open, and needing you

1. **The engine evaluates the LATEST checkpoint; every banked rate came
   from a PRESERVED PEAK.** That is why 1-3's endpoint read 0.0 while its
   ledger entry stands at 21%. The evals the engine produces are honest
   measurements of the endpoint, not attempts on the banked claim, and
   should not be read as contradicting CLAIMS.md. Fixing this means
   teaching the engine which checkpoint a level's claim rests on.
2. **1-1's 43% still has no receipted entry** — see
   `~/nes-mission/memo_2026-08-18_ladder_and_ledger.md`.
3. **1-4 ran on an inert ladder**; its 51% stands but is not evidence the
   curriculum transfers.
4. **`nes_core.abi3.so` predates `cpu_asm.rs`.** The replay sweep gating
   the rebuild is queued above.
5. **`test_night2_runner.py::test_dry_run_passes_live` red since
   2026-08-15**, deliberately unfixed.

## Engine defects fixed today, for the record

Each was silent and each would have wasted the window: launching into an
occupied lane; a global slot that idled the engine for 5.5 hours; a
misfiled lane that outlived the tick that made it; `--glob` treated as a
literal path, making the replay sweep permanently un-runnable; an eval
counted as current after the policy changed under it; required CLI
arguments never checked; and benchmarks run on a hot machine.
