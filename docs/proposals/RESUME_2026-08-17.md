# RESUME — state at the 2026-08-17 host reboot

Everything below is on disk and survives the reboot. No process needs to
be recovered; all emulator work was stopped cleanly and every result is
either committed or preserved with a hash.

## Do this first, in order

1. **Bank World 1-4.** A consolidation probe scored **18/30 strict
   (60%)** — the highest rate any level has produced — but the
   definitive eval had not run when the machine went down. Until it
   does, 60% is one 30-episode probe, not a claim.

       for SEED in 7 101; do
         .venv/bin/python scripts/eval_game.py --game mario_1_4_online_v1 \
           --profile configs/mario_1_4_online_v1.yaml \
           --rom "roms/Super Mario Bros. (World).nes" \
           --checkpoint checkpoints/_preserved/one_four_MEASURED60_iter00960.pt \
           --episodes 50 --max-steps 3000 --sequential --level-clear \
           --start-state checkpoints/super_mario_bros_one_shot_tiles/smb_curriculum/stage_10.state \
           --eval-seed $SEED --sticky-prob 0.25 --start-jitter 16 \
           --eval-workers 5 --eval-rng per-episode > runs/online_1_4/final_eval_seed$SEED.json
       done

   Then write the pooled strict rate into `CLAIMS.md` alongside 1-1
   (43/100), 1-2 (38/100) and 1-3 (21/100), and append the result to
   `runs/online_1_2_attempt_ledger.md`, whose 1-4 section already
   carries everything except the final number.

2. **Three analyses that need no training at all** (from
   `docs/proposals/RESEARCH_SYNTHESIS_2026-08-17.md`):
   - the envelope estimator λ = −ln(C)/L across all four levels, plus
     the death-position-variance diagnostic, which says per level
     whether more campaigning is worth anything;
   - SWA over 1-2's existing checkpoints around its 12/30 peak;
   - the shared-substrate falsifier (one base + four heads on 1-1..1-4)
     against the isolated baseline we already own.

3. **2-1 is staged** — `docs/receipts/two_one_runbook_2026-08-17.md` has
   eight copy-paste commands, ~10 minutes, with its competence floor
   deliberately left disarmed so the calibration step cannot be skipped.

## Where the levels stand

| level | banked | policy |
|---|---|---|
| 1-1 | 43/100 strict | `_preserved/backward_1_1_best_honest_greedy_047.pt` |
| 1-2 | 38/100 strict | `_preserved/consol2_40pct_strict_iter01120.pt` |
| 1-3 | 21/100 strict | `_preserved/one_three_FINAL_consol2_iter00690.pt` |
| 1-4 | **pending eval** (60% on one probe) | `_preserved/one_four_MEASURED60_iter00960.pt` |

Eighteen checkpoints preserved with sha256s under `checkpoints/_preserved/`.

## Research: five rounds, all returned

`docs/proposals/RESEARCH_SYNTHESIS_2026-08-17.md` is the synthesis.
Headline: three independent rounds converged on a **discrete-time hazard
model** trained by survival analysis on our own deaths, with a gated
three-phase build order. Also settled: RPGs are off the roadmap for lack
of any purity-legal guiding signal; the previous-action feature's
failure is causal confusion; capacity scaling has negligible online
effect; a comparison-shaped outcome vocabulary exists with R.C. Pro-Am
as its decisive first experiment.

Reports live privately in `~/Documents/research-consult/responses/`
(v16 room-graph, v17 outcome vocabulary, v18 hazard substrate,
v19 combat classes, v20 rate ceiling). Gemini budget: roughly $60-70 of
$100 remaining.

## Standing findings not yet acted on

- **2 of 8 banked solver tapes do not replay from their own recorded
  root** (1-3 sol_001, 1-4 sol_003). Both quarantined; a full sweep of
  every banked solution is queued and unbuilt.
- **The DMC/ASM fix is committed but the loaded `.so` predates it.**
  Rebuilding requires a replay-integrity sweep and a PGO refresh first
  — see the migration gate note in the campaign memory.
- **The Anthropic API monthly spend limit was hit**, which blocks
  subagent and workflow fan-out until it resets or is raised. Direct
  session work and the Gemini research bridge are unaffected.

## The League mission

Its own private repo at `~/nes-mission` (committed). The 24-hour oracle
soak passed with zero interventions and was adjudicated PASS with three
honest findings recorded at equal prominence — including that the stall
clause passed *vacuously* (no stalls occurred, so self-arming was never
demonstrated) and that the adjudicator itself had a blind spot the
adversary found and closed. Mission sessions are launched detached and
resume from `~/nes-mission/MISSION_STATE.md`.
