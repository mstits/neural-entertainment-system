# STRATEGY 2026-08-08 — Final Determination (post red-team)

## Determination

**Primary thrust: ship the self-unsticking solver, with the record protected and the wins made visible.** The basis-set conversion thesis survives the red team; its *scheduling* did not. Three material reversals from the draft, all evidence-checked against disk before acceptance:

1. **Contra is out as the Day-30 headline.** Verified: ten campaigns in `runs/breadth_contra/` (162 GB), every `solutions/` empty. Putting the month's only visible milestone on the project's worst-converting game, with its death byte still unresolved, was the sunk-cost error pointed outward. Contra stays as the class-5 *research* lane under elimination-ledger discipline: prior stated up front (0/10 campaigns), what is materially different named in writing (detector v2 clear-hook + replay-verify, the first Contra attempt with trustworthy termination), stopping rule registered before launch, gate = telemetry (frontier past the base wall + named wall class), not a clear.
2. **Bubble Bobble finish is in as the Day-30 visible deliverable.** Verified: `chain_day2c/chain.jsonl` ends `{"level":"60","status":"solved"}` with `entrance_after_60.state` on disk (Aug 6). ~40 rounds of an already-working attended loop converts the number any observer counts — games beaten: 1 → 2 — with zero new mechanism work. Riders: EXHIBITION label per CLAIMS.md; the 1P-short-ending caveat announced *before* the run.
3. **The pooled n=240 binomial for B5 is killed.** The prereg registers per-cell reporting (greedy×sampled×seed, warp_rate 0, Tier-2 = ≥1/30 on both seeds) and greedy-vs-sampled is the project's decisive discriminator (B4: greedy 0.02 vs sampled 0.50/0.63 on the *same net*). Pooling non-exchangeable cells would manufacture exactly the false positive the 07-21 pivot recorded. Extra power, if wanted, is bought with more episodes per cell — eval is ~3.5% of gate cost.

**Prereg-deviation rule (reconciles two critiques that pull opposite ways):** deviations are permitted only as written addenda published before launch; they may loosen *procedure* (B5 runs despite B4's failed greedy gate, with the gap as a registered caveat) but never the *statistical bar* (per-cell cells and thresholds stand as registered). The B4 gate as drafted was a trapdoor — B4 v2's verdict already exists (0.02/0.02, identical to v1) and the entropy guard was never in a 1-1 config — so gating B5 on it meant learning demoted *by default, with no verdict*. Worst outcome: cost paid, information not bought. B5 runs once, under addendum.

## Primary thrust, sequenced

- **Week 1, in this order (opens with a win, not a wake):** (a) Monday, hours: push the 40 unpushed commits; draw the receipts-vs-working-data line; second copy of claim-backing receipts. (b) First attended block: resume Bubble Bobble from `entrance_after_60.state`. (c) Learning closure: fix the TauScheduler checkpoint MAJOR; ONE v4 consolidate_level attempt (1h box, per the campaign's own recorded recommendation); write and publish the B5 prereg addendum; run B5 seed 0 in its attended ~2.5h window, seed 1 the next day (5h+ attended — the draft's "3.2h" undercounted the registered protocol). Same-day refutation pass on the result. (d) Put the **FORGE ledger** ask before the user (below).
- **Week 2:** B5 verdict published either way, per-cell table, pooled line descriptive-only beneath it. Detector-trust fix (moved from week 1 — the draft's week-1 load had no slack): `is_dead()` before `is_clear()`, combat-blip + room-transition modes, replay-verification before any `solutions/` banking; gate = attended re-run of the 08-06 four-game batch, zero fabricated clears. Fidelity bisect (fork ~4118→2000-2950) resolves by day 14 — load-bearing for the hall verdict and the event-driven-PPU flip both.
- **Weeks 2-4, show cadence:** two attended show blocks/week, standing. They are simultaneously Bubble Bobble's compute, the content supply, and legal under no-overnight. Ship the two smallest demo-flag items from the user's own backlog so a show night is one command. (Rejected: auto-wiring clips into every gate — automation creep for a one-person shop; run `daily_recap.py` manually after gate days.)
- **Weeks 3-4:** CV hall, one pre-registered arm = fidelity-corrected + ortho, prior stated (~110M steps, 0 solutions, 5 arms), stopping rule registered. Contra research lane as above. "CV 90%" moves to the FORECAST ledger and stops justifying allocations; the honest statement is "blocks 0-2 of ~18."

## 30-day gates (each can fail on its face)

- **G1 (day ~21):** Bubble Bobble 100 rounds receipted + victory lap. Fail → the chain loop itself is broken; diagnose before any breadth.
- **G2 (day ~10):** B5 verdict published as registered. Tier-2 pass on both seeds → phase-2 backward robustification becomes a first-class lane. Fail → learning demoted to bounded quarterly bets; eighth-family rule (no new family without a genuinely new, externally-sourced idea) becomes standing policy.
- **G3 (day ~14):** detector v2 + replay-verified banking shipped; four-game re-run clean. Fail → no new banking anywhere until it is.
- **G4 (day 30):** ≥1 receipted clear across {CV hall, Contra}. Fail → new-class breadth halts for the quarter, and the "classes 3-7 reuse infrastructure" forecast is publicly downgraded in CLAIMS.md. No "or reclassified" escape hatch: reclassification is a finding, not a pass.

## 90-day shape

- **Days 30-60:** T1 v0 (stall watchdog → auto diagnosis bundle → agent invocation), narrated live as FORGE — this is the show's differentiating segment and it is *true as stated*. Mega Man starts when Contra converts OR its two-week escalation window expires with the wall classified in writing (the conversion gate is now time-bound — the draft's unbounded lock deadlocked its own falsifier #2). Tetris-B lives in a cheap-legible-win lane *exempt* from the conversion gate. **Day-60 gate:** ≥4/8 basis classes with receipted show-mode clears — "in-flight-with-receipts" struck (CV has been in-flight since 07-27 without converting; the clause passed trivially). This gate is deliberately at-risk; if it fails, the basis set is publicly re-scoped.
- **Days 60-90:** Archive Engine v2 (2-3 days) strictly before Metroid's room-graph workload. Punch-Out. League decision point with the user (attended League-night blocks vs. amending the no-unattended directive — policy call). Learning: phase-2 iff B5 showed signal; else at most one bounded DR-vetted bet.

## The product-identity question (goes to the user this week, as a trade)

The draft's "third option" claimed "an AI that learns itself is true today" — that launders EXHIBITION-class output across the exact ledger boundary CLAIMS.md defends, and the two-ledger discipline is the moat. Revised ask, smaller and honest: **add a third ledger, FORGE** — "the system diagnosed its own wall from self-measured telemetry and built a new mechanism, unaided" — with the words learn/learned/self-taught banned for FORGE-class results on every public surface. "Nobody touched this — the machine noticed it was stuck and wrote itself a new search arm" is a distinctive claim and true as stated. The ask must also answer, in writing, CLAIMS.md Tier-3's "LLM guidance of exploration" clause as applied to T1/T2 (the tension already exists in practice — the hall arm shipped agent-forged). Provenance labeled honestly: the reframe was reconstructed after lane E5 returned nothing. **Nothing else in this plan depends on the sign-off; every lane proceeds regardless.**

## Stops

1. No eighth learning method family; B5 runs once under the registered addendum; no v4/v5 cycling beyond the 1h box.
2. SIMD 6502 stays killed (own derivation: 1.14× full-stack).
3. Actor/learner overlap does not start within 90 days.
4. Conversion gate, time-bound: next class starts on receipted clear OR after two weeks' escalation with the wall formally classified in writing — whichever first. Cheap-legible-win lane (Bubble Bobble now; Tetris-B fallback) is exempt.
5. No League until detector v2 + auto-recovery ship AND the user resolves the unattended-compute conflict.
6. Delete/archive the Dreamer dead scaffold.
7. No new proposals/dossiers without a conversion receipt attached to the last one; forecast numbers live only in the FORECAST ledger.
8. Never cut the same-day refutation pass (four instrument-was-the-bug incidents in four days; one unrefuted instrument cost eight days of a headline metric).

## Falsifiers (instrumented)

1. **B5 Tier-2 pass as registered** → learning re-promotes toward primary thrust; this memo's demotion is void.
2. **Contra walls despite full escalation + T2 forge by day 45**, and Mega Man (permitted under the falsifier-testing carve-out in stop 4) also walls → the compounding thesis is wrong; pivot to a depth product — SMB + Bubble Bobble (beaten by then, correcting the draft's retreat position) + CV-as-far-as-it-goes.
3. **Detector v2 work proves generic clear-detection needs per-game bytes** → the League as specified is dead; totality redefined over the hand-adapter basis; 793-library framing retired.
4. **Bisect finds real Mesen divergence altering solver reachability** → fidelity jumps above basis conversion; integrity outranks throughput.
5. **Two consecutive scheduled show blocks with nothing bankable to show** → re-prioritize toward the cheap-win lane. (Replaces the draft's audience-attrition falsifier, which named no instrument and could never fire.)
6. **Refutations that overturned a recorded claim, tracked in CLAIMS.md, exceed 2/week above the trailing baseline** → halt feature work, harden instruments.

## Objections considered (disposition summary)

- **Contra/CV sunk-cost inversion — ACCEPTED** (evidence verified on disk). *Partially rejected* for the hall: the fidelity bisect is a genuinely new attack the prior ~110M steps did not have, so one pre-registered post-bisect arm is warranted — under stated priors and a stopping rule, not under "90%".
- **Pooled-binomial amendment — KILLED as proposed.** Per-cell prereg stands; pooled lines descriptive-only.
- **B4-gate trapdoor — ACCEPTED**; *rejected in part:* B5 does not run "unconditionally" — it runs under a written, pre-launch prereg addendum. Post-hoc looseness condemned in one critique cannot be demanded raw in another; the addendum rule reconciles them.
- **Gates-cannot-fail — ACCEPTED**; all gates rewritten failable; falsifier-#2 deadlock fixed via the time-bound + carve-out.
- **Third-option vocabulary laundering — ACCEPTED** via the FORGE ledger; the "learning is true today" sentence is retracted.
- **Bubble Bobble omission — ACCEPTED** (the sharpest catch in either brief; verified to the byte).
- **Show falsifier/cadence — ACCEPTED** (cadence + demo flags); *rejected:* gate-triggered clip automation, and the framing that the only alternatives were audience-metrics or deleting the show identity.

## This week

1. Push the 40 commits; receipts line + second copy. (Monday, hours.)
2. First attended block: Bubble Bobble resume from round 60. Announce the 1P-ending caveat on stream before it.
3. Learning closure arc: TauScheduler fix → 1h v4 box → prereg addendum published → B5 seed 0 (attended 2.5h), seed 1 next day; same-day refutation pass; verdict published per-cell either way.
4. Detector-trust fix started (lands week 2 with the four-game gate).
5. FORGE-ledger + Tier-3 ruling put before the user in writing.