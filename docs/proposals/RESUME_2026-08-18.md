# RESUME — 2026-08-18 evening

## STOPPED — everything was shut down at the end of the session

Nothing is running. The LaunchAgent is installed at
`~/Library/LaunchAgents/com.stits.nes-engine.plist` but **unloaded**.

To restart in the morning:

    launchctl load ~/Library/LaunchAgents/com.stits.nes-engine.plist

That alone is enough — the engine will re-plan from filesystem state and
pick 2-1 back up. Nothing else needs launching by hand.

**Where 2-1 was stopped:** phase 5 (consolidation), last checkpoint
`vanilla_ppo_iter_00940.pt`, last honest probe at 86.2M with median
max-x **2559.5** and strict 0.0. That median is worth noting: it was
~1620 through the reverse walk and 1016 before it, so consolidation moved
it to roughly 78% of the level (flag at gx 3298) in about 5M steps.
Strict is still zero, so nothing is claimed — but the trend is the one
that preceded a rate arriving on 1-2, 1-3 and 1-4.

Resume 2-1 specifically with:

    .venv/bin/python scripts/detach.py --log runs/online_2_1_console.log -- \
      .venv/bin/python scripts/run_online_campaign.py \
      --campaign-config configs/campaign_2_1.yaml --start-phase 5

## What was running before the shutdown

- **2-1 consolidation** — detached, resumed at 81.1M cum steps via
  `--start-phase 5`, following the precedent that produced 1-4's banked
  51%: `online_1_4` skipped phases 3 and 4 entirely.
- **The engine driver**, under launchd (`com.stits.nes-engine`,
  `StartInterval` 600, `RunAtLoad`). It ticks every 10 minutes, survives
  reboot, and is now the ONLY tick source — the earlier detached loop was
  retired so two sources could not race into a double launch.

Stop it with:

    launchctl unload ~/Library/LaunchAgents/com.stits.nes-engine.plist

## What the engine will do on its own

Priority order, computed from filesystem state each tick:
score a finished campaign (honest eval, two seeds) → hazard Phase-1
benchmark → full replay sweep → extend the level pipeline. While an
emulator job holds the machine it runs token-bound work instead of
idling; it will never launch a second emulator job.

Expected next: when 2-1's consolidation ends, two honest evals
(seeds 7 and 101), then the hazard Phase-1 gate.

## Known limitation to fix first in the morning

`hazard_phase1`, `replay_sweep_full` and `suite_check` have no
filesystem-derived completion check, so nothing marks them done. They
will re-run until `MAX_ATTEMPTS_PER_ACTION` (2) and then be abandoned.
Bounded and safe — no runaway — but wrong: `suite_check` in particular
should be repeatable rather than capped. The fix is a `completed` write
when a launched action's process exits cleanly, plus an exemption for
recurring tasks.

Everything driven by `honest_eval_done`, `level_has_campaign` and the
W-chain resolves correctly, because those read the filesystem.

## Open, and needing a human

1. **1-1's 43% has no receipted CLAIMS.md entry** — prose roll-up only.
   The measurement is real (`runs/interference/interference.jsonl`, 100
   episodes, `clear_rate_strict` 0.43). See
   `~/nes-mission/memo_2026-08-18_ladder_and_ledger.md`.
2. **1-4 ran on an inert ladder.** The 51/100 stands on its honest eval,
   but 1-4 is not evidence that the backward curriculum transfers.
   Falsifiable: re-run on the corrected ladder; if the ladder does work,
   51% rises. Config is fixed and its W2a arithmetic re-registered.
3. **`test_night2_runner.py::test_dry_run_passes_live` has been red since
   2026-08-15.** The night2 guard correctly refuses to seed over 32
   newer checkpoints; the assertion went stale when that workload ran.
   Left failing on purpose — weakening a test to green it is mission
   failure.
4. **`nes_core.abi3.so` predates `cpu_asm.rs`**, so the committed DMC/ASM
   fix is not in the running binary. The replay sweep that gated the
   rebuild is now built and partially run.

## Settled today, worth not re-litigating

- The two tapes carried for days as "do not replay from their own root"
  (`ge_1_3_solve/sol_001`, `ge_1_4_solve/sol_003`) **replay fine** —
  frames 530 and 443. They were never broken, only unverifiable for want
  of a recorded profile. 1-2's real ladder tapes sweep 6/6 green.
- 2-1's reverse walk reached tau=1 of 6 (4 advances) and exhausted its
  pre-registered 60M budget one rung short of `restart_at_entrance`.
  Both levels that have ever run phase 3 died in it, and the one banked
  level that skipped it has the highest rate. Whether phase 3 earns its
  budget is the cheapest available shortening of the validation cycle.
