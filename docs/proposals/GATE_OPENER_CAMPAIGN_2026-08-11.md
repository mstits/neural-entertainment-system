# GATE-OPENER CAMPAIGN — 2026-08-11 (MASTER SYNTHESIS, rev 4 — post-red-team final)

Rev 4 amends rev 3 in place under the ten final red-team objections; every objection is dispositioned in §10 (all ten verified against source/disk before disposition). Rev-2/rev-3 dispositions stay binding EXCEPT where §10 supersedes them: B2 (same-sidecar two-arm A/B) is superseded by the three-arm design (§2), K2b's 0.85 threshold is superseded by 0.95 (§7), and the §0-repair-2(b) CV-answer tuning wall is superseded by Contra-only tuning (§4a).
Rev 3 supersedes rev 2's Hybrid C+B decision. Final six-design scoreboard: A-interaction-enum 14.5, E-mini-dispatcher 12, F-minimal-diff 10, C-contact-scanner 8.5, B-entity-kill 4, D-temporal 0. Decision = HYBRID A+E. Rev 2's ten red-team dispositions (§8 of rev 2) were each re-verified against source and remain BINDING where cited below (sidecar routing B2, gate_marks B3, count_wmax B4, macro precedence B5, flat_windows/ARM-FAILED B1, K0 revision cap A3, K1b symmetric-coverage kill A3, G2 deletion A1, Contra UNTYPED A2, burst prereg A5). gate_opener_arm_2026-08-11.md (828 lines) is a starting point only; where it conflicts with this doc, this doc rules.
Wall class: GATED-candidate (heavily-concentrated position coverage; hypothesis: some STATE must change before a boundary opens). "Saturated" is STRUCK pending K-FALSIFIER (§7, §10-O1): concentration is a coverage DESCRIPTION that rises with runtime, not a wall property — banked SOLVED archives score 4–11× CONCENTRATION_GATED_MIN and escape GATED only via the earlier RESOLVED/PROGRESSING branches (verified in gated_wall_verdict source: branch order 2/3 precede branch 7/9). Discriminator src/training/wall_taxonomy.py (runtime-inert, 58 tests). Carried caveat: ZERO confirmed gated walls; positive class = the CV hall scored three times (docs/receipts/dispatch/gated_wall_calibration_2026-08-10.md §0/§9); class PENDING-VALIDATION; CV = sole class-definer; the hall's own score is a monotone clock (31.04→85.20→110.53→136.34 while distinct_spatial sat 932–1,094).
Targets: (1) CV block-3 hall — pin gx 767; runs/cv_hall_ortho_a (131,561 cells) + _ctrl (149,153 cells; 14 band cells in bucket 95, re-frozen — not 18); resume archive = runs/cv_chain_hw/lvl_03_overnight (560,410 cells, best 767, 45.9M steps, 5.98 h, 11.9 GB, 0 solutions — the deepest banked attack, previously omitted; §4b lineage check FIRST, fallback cv_chain_hw2/lvl_03_trace). Pin history restated: FIVE runs / ~10.7 h / ~77M steps / 0 crossings (not four/4.7 h). PRIMARY. (2) BB 99-1 — runs/bubble_bobble/r99_1_boss_retry (9,475 cells, KEY_BLIND spatial_span=1); entrance runs/bubble_bobble/chain_day2g/entrances/entrance_after_99-0.state; role = instrument control + KEY_BLIND repair, never a GATED crack. (3) Contra base — runs/contra_reentry_2026-08-10/r1_ortho, frontier AT gx 3072, 52,539 cells: registered UNTYPED; the frozen gate re-read returns VOID/under_covered (distinct_spatial 1,205 << floor 7,427) → DECLINE TO ARM; 15-min instrument probe only.

## 0. DECISION — HYBRID A+E ("enumerate the interactions, ledger the candidates, promote between runs")

THESIS (A, reworded per §10-O1): the archive shows every position VISITED heavily (ctrl concentration ≈136 records/spatial-bucket — a visit count, NOT proof of saturation), and shows NOTHING about which INTERACTIONS were tried there. A full controller sweep from archive savestates is affordable, so sampling it is a mistake. The sweep's value is as a DISCOVERY INSTRUMENT and does not depend on the GATED label surviving K-FALSIFIER.
- KEEP from A (winner): deterministic interaction basis (pure module), scheduled in-loop sweep off archive blobs, full-2KB differential diff with paired NOOP subtraction + cross-root invariance + TIMED/LATCHED/COUNTER classing + novelty ranking + BH-FDR q=0.01, the T1/T2/T3 gate lattice, sham-root null (K1), Contra decline.
- KEEP from E: SHADOW AXIS LEDGER (candidates never touch the cell key in-run; arity stays frozen at (sect,tb,kk,psig,loops,route_sig)+cell_fn all run, so cells/new_cells stays A/B-valid); promotion to real state_sig bits only BETWEEN runs via a --gate-axes sidecar merged after profile load (:1257) before make_game (:1263), appended so resumed bit indices survive, BOTH arms loading the same sidecar (rev 2 B2, verified: state_sig is read ONCE at :637–650, consumed at :946 — no runtime admission exists); gate_marks 8th trace element (:1653 stores 7-tuples; :2576 unpack length-tolerant — rev 2 B3); inline paired control in the same pool (no serial-only control); per-candidate receipt files; probe/ctx isolation.
- DROP from E (judge flaw 2): the adaptive UCB family scheduler and multi-family bandit — unvalidated machinery atop an unbuilt arm. v1 uses A's fixed sweep cadence + fixed budget split. DROP E's F-TEMPORAL constants (48/190/420 — judge flaw 1: BB $0030 protocol verbatim, a researcher prior in the TIMED/LATCHED knob). DROP A's key-slot-2 riding and its --kill-key exclusivity: superseded by the sidecar (no in-run key change at all; co-arming with --kill-key allowed per rev 2 B5 — :2712 guard writes only c['kills']).
- F's verified-but-losing spec donates its no-false-claims line-number map (all 58 verified) and one binding correction: room_advance{addr:0x0462} for BB is a NO-OP as specified (derive_transition_macros :1157 shape mismatch) — $0462 is a downstream T3 detector, never a candidate or macro target. C's contact-only scope and B's hardcoded-$0030-subgoal purity breach are rejected whole.

FLAW REPAIRS (judges' fatal flaws on A, both honored):
1. THROUGHPUT ×8 (A flaw 2, verified in source): sps at :2814 divides steps_done, which increments += args.workers per pool step (:2675) — it is AGGREGATE 8-worker rate. Receipted aggregates: CV 1,661; Contra 3,202,552/1,800 s = 1,779; BB 1,805 steps/s. Every budget in §6 is recomputed at 1,700 aggregate (≈212/worker); A's "~7 s/root" is really ~55–60 s at its own parameters, so the basis is cut (§1): pass-A phases 4→2, roots 24→16, repeats 3→2; full 4-phase/512-tail reserved for pass-B survivors.
2. K0 CIRCULARITY (A flaw 1, tuner/grader wall REBUILT per §10-O5 — the rev-3 wall separated by ANSWER, not by TARGET; a ranker revised until a low-zero-page mode bit ranks ≤5 on a CV stair root is biased toward CV-shaped bytes, so a hall hit would not be independent): (a) ALL basis constants are fixed design ladders declared here, derived from no target: holds {4,12,36,108}, tap duties {(2,2)×8,(2,10)×4}, settle phases {8,21} (pass-A) / {8,13,21,34} (pass-B), tails {24 pass-A, 512 pass-B} as a geometric ladder 24→512, FDR q=0.01, rank cutoff 5. (b) TUNING happens on a THIRD game only: the Contra rediscovery probe (receipted hp 0x04BF–C2 / ident 0x058F–92 bytes, §4d — promoted from side-check to the ONLY tuning case; different ROM/profile/action-space/zero-page layout from both graded targets). ≤2 revisions, each may consult Contra answers only; revision count + tuning target logged in the sidecar header. (c) After freeze, BOTH known-answer grades run BLIND, once each: CV $0004==8 (stair mode-bit, lvl_02 root) and BB $0030 (possession, 99-0 pickup root); both answer keys withheld from the tuning pass and from all exclusion lists. A post-freeze miss on either → campaign disarms (no third revision). Every citation of a K0 PASS must name the game it was tuned on (Contra). The known bytes are grading truth ONLY — never inputs to any sweep (B's breach).

## 1. IMPLEMENTATION SPEC (default-off; byte-identical when off; mirrors --ortho)

NEW FILE src/training/interaction_basis.py — pure: interaction_basis(action_space, cap=128) → [Pattern], Pattern=(name, family, tuple[mask]); deterministic from the profile's DECLARED action list; families HOLD/TAP/COMBO(pairwise OR of declared masks, deduped, cap 24, never invents a button); slot 0 = all-NOOP program of equal length (the paired control). GENERATION TABLE is FIXTURE DATA (§10-O8): family order HOLD→TAP→COMBO; dedupe by mask-tuple, first occurrence wins; truncation (if cap ever binds) drops from the COMBO tail; per-target counts asserted by test: CV(|A|=16)→120 (64 hold + 32 tap + 24 combo), BB(8)→72 (32+16+24), Contra(11)→90 (44+22+24) — rev-3's 128/66/88 were WRONG (not derivable from the declared ladder) and are struck. CONTACT is NOT in the pure basis (state-dependent, cannot live in a function of action_space alone): it is a sweep-time admission of the hold-108 patterns, evaluated on live RAM during settle with declared self-measured parameters — position observable = the profile's own gx/y telemetry the solver already reads, eps=0 (|Δgx|=0 AND |Δy|=0), K=8 consecutive steps — all three echoed in the run header. Program = settle + pattern + tail, NOOP-padded to one uniform length of 153 steps (max settle 21 + 108 + 24 tail; rev-3's "uniform 140" used min-settle and is struck; §6 recomputed +9%).
SOLVER (scripts/go_explore_solve.py):
- Flags: --gate-opener {off,enumerate} (rev-3's search/both values CUT — 2 of 4 values had no specified behavior (§10-O6); a search mode returns in v2 only with its own spec); --gate-sweep-frac 0.10; --gate-sweep-roots 16; --gate-sweep-repeats 2; --gate-pin-secs REQUIRED when mode≠off (explicit, no derivation — §10-O9; mined defaults documented: 600 CV / 120 BB; negative disables — tested); --gate-target-typed (explicit operator attestation that the target has a typed corpus row; echoed in header; replaces §3's unimplementable "TYPED in the corpus" runtime conjunct); --gate-band 24; --gate-sham-roots 4; --gate-axes PATH (sidecar); --contact-bits 3 (cap on merged sig axes, assert len(state_sig)≤8). All forwarded by go_explore_chain.py (it forwards neither --kill-key nor --ortho today — the receipted dead-flag lineage: eval --start-state, sect-cap; chain hardcodes 12 flags).
- Solver._gate_sweep(), fired from explore() (:2592) on a step-count cadence sized to --gate-sweep-frac, at BURST BOUNDARIES ONLY (a worker is swept only when its ctx has left==0, i.e. its burst just completed and before its next _assign — no in-flight steps or in-flight macros are ever discarded; §10-O7): roots = 16 least-swept band cells (key[-1] ≥ topgx−gate_band) + 4 sham roots from the freely-advancing region. Per wave: pool.load_worker_state(wid, blob) (archive blobs = free reset), step_all through the program, RAM captures at t0/t1/t2 (settle/pattern/tail end). STATE ISOLATION (binding — rev-3's "reassign via _assign then assert ctx byte-identical" was contradictory by construction, since _assign calls select() and load_worker_state and draws from self.rng (:2555–2589)): before sweeping worker i, save blob_i=pool.save_worker_state(i) and snap_i=deepcopy(ctx[i]); after the wave, pool.load_worker_state(i, blob_i) and ctx[i]=snap_i verbatim; NEVER call _assign from the sweep path; NEVER touch cell.barren or yielded bookkeeping; the sweep uses its OWN np.random.Generator (seeded run-seed⊕"gate") so the search stream is untouched. Unit test (3 parts): ctx dicts equal, worker savestates equal, and self.rng.bit_generator.state unchanged pre/post sweep.
- Solver._gate_rank(), pure, per root over the FULL 2KB (no allowlist): onset = ram_t1≠ram_t0; persist = ram_t2≠ram_t0; NOOP SUBTRACTION drops any addr moving in slot 0 from the same root (kills frame/RNG/timer/music free-runners); CROSS-ROOT INVARIANCE requires same (addr,family) from ≥3 distinct roots; class onset-only=TIMED, both=LATCHED, monotone=COUNTER(nuisance). Pass-B re-runs survivors (≤16 patterns) at 4 phases + 512 tail from 3 confirm roots to fix persistence class (a 24-frame tail would misclassify slow-revert LATCHED as TIMED — ladder rung, not BB-derived).
- NOVELTY: Solver._boundary_hist, 2048×256 uint32 (2 MB), updated in observe() at 1/32 stride for band cells only; novelty(addr,v)=log2((H_tot+1)/(H[addr][v]+1)). RANK = novelty × w_persist(LATCHED 1.0/TIMED 0.6/COUNTER 0) × controllability × (1−farmability); refuse >8 values or >1 event/1k random steps; BH-FDR q=0.01 over the addr×family grid; sham-root yield reported beside wall-root yield (K1 input).
- SHADOW LEDGER (E): observe() keeps shadow[cand]={(bucketed value, y_band, gx_bucket)} for admitted-candidate addrs in-band; shadow_yield=|shadow|/distinct_positions = cells the axis WOULD create. No key change in-run. gate_marks=((step,cand,kind),...) appended as trace element 8 at record() (:1651/:1672); resumed 7-tuple archives load unchanged.
- AXIS ROUTING (rev 2 B2, binding): --gate-axes loads docs/receipts/probes/gate_axes_<target>.json, merges entries into profile['solve']['state_sig'] between :1257 and :1263, APPENDED last; --contact-bits enforced at merge; merged list + sha echoed in header; a sidecar entry lacking a probe-receipt sha is REFUSED at merge (no agent can hand-inject a byte); CONTROL RUN LOADS THE SAME SIDECAR (else cells partition differently and every A/B metric dies); search-without-sidecar degrades loudly to enumerate.
- ACTION PRECEDENCE (rev 2 B5): sweep programs and any grid entries use the ONE shared macro slot (c['macro_a']/['macro_hold']/['macro_left'], 6-step settle; transition_macros :2635 / hold_macros :2653 & :1301); precedence at slot-acquisition only: gate > transition > hold; in-flight macros never preempted (CV's up×24 p=0.02 stays live); a hold that cannot complete in the remaining burst is not started.
- count_wmax(dw, ow, gw=1.0) default-arg form (rev 2 B4): tests/test_go_explore_solve.py:863–873 2-arg contract holds; the two source-text assertions at :1133 updated same commit; 64-draw exhaustion rate (:2451–2469 fallback) test-pinned and header-recorded.
- Telemetry (armed only): gate_{armed_secs,sweeps,programs,candidates,admitted,sham_yield,cross,lift_active,lift_ctrl}, boundary_state_axes, alias_ratio; run header records argv/commit/seed/workers/hw-flags/root-sha/burst/ACTIVE-predicate/sidecar-sha; stdout teed.
TESTS OWED (28): 8 solver (arm short-circuits; RNG last; args.<dest> reads; count_wmax exact w/ gw; arity frozen; default-off byte-identity; empty-pool fallthrough; telemetry-only-when-armed), 6 basis (determinism against the fixture generation table, per-target counts 120/72/90, cover, no-invented-mask, uniform length 153, slot-0 NOOP), 5 sweep/rank (3-part state-isolation: ctx-dict equality + savestate equality + search-rng state unchanged; sweep-rng independence; NOOP-subtraction; 3-root invariance; rank stability under permutation), 3 gate_marks/suppression (hand-built ctx+trace; masked window changes emitted bytes; 7-tuple resume), 2 precedence/grid-burst, 4 receipt/provenance (sidecar-sha refusal, chain forwards all --gate-*, taxonomy inert, negative --gate-pin-secs disables). Pre-commit: make test + make parity (146 Mesen tapes/554 tests) + cold eval composite_w3_final seq_clear 1.0 (SMB regression guard; NMI offset=1 stays UNSHIPPED).

## 2. GATE-TEST (staged lattice; frozen at arming: pin_gxb + candidate table + ACTIVE predicate in the header)

T1 KEY-LIFT + INJECTION-LIFT (three arms; rev-3's two-arm design put the sidecar — the actual treatment — in BOTH arms and handicapped the armed arm with the sweep tax on the deciding metric, so it tested nothing it was named for; §10-O6; supersedes rev-2 B2): arms at equal wall-clock (90 min each, sequential, same seed/config/burst/bucket): (A) no sidecar, arm off; (B) sidecar merged, arm off; (C) sidecar merged + sweeps + macro injection. B−A = key lift; C−B = in-run injection lift. SCORED ON replay-verified crossings (frontier gx bucket > pin_gxb, replay_verify :1675 green) and banked solutions ONLY — never cells (R9; cells are incomparable across sig schemas, which was rev-2 B2's whole reason to equalize the sidecar, now moot). A crossed too → coverage_opened_it, never a crack. CROSSING-FREE SECONDARY ENDPOINTS scored on all arms (§10-O3 — the pin history is 5 runs / 10.7 h / 0 crossings, so a crossing-conditioned lattice alone would likely report nothing): shadow_yield, BH-significant admitted-candidate count, and shadow-cell attainment (reaching band cells the frozen key cannot represent). TWO SEEDS PER ARM are pre-registered as required for any GATE-OPENED claim; this week funds seed 1 and (D5, protected) seed 2 of the decisive pair — if D5 is lost, the week yields a SINGLE-SEED result and cannot close G1 (stated up front, §5). Any replay-verified clear BANKS via _dump_solution and re-chains regardless — attribution downgrades the mechanism claim only, never the clear.
T2 SUPPRESSION (causal necessity; extends counterfactual_probe :1771, same receipt shape). Rev-3's fixed K=16 McNemar at p<0.01 is KILLED (§10-O10): it needed ≥7/16 discordant one way, i.e. >44% policy-resampled crossing recurrence from the onset snapshot, on a boundary that by definition almost never opens — an unsatisfiable bar with no power calc behind it. Replacement: (a) during K0, measure recurrence base rate p0 from ≥50 policy resamples off a KNOWN crossing onset (the CV stair-mode transition — it crosses reliably, so p0 is measurable); (b) set K by power calculation for the pre-registered MDE (detect recurrence drop ≥0.3 at exact-McNemar p<0.01); (c) if p0 < 0.15, the distributional test is unpowered at any sane K — run the DETERMINISTIC ABLATION instead: verbatim replay with pattern frames NOOP-masked, replay_verify (:1675) on both traces, reported as NECESSITY-ONLY (no distributional claim). Deep-retry separates state_artifact from control-blind (rev 2 carried).
T3 SUFFICIENCY (injection): ≥30 held-out band roots that never crossed; paired arms from the SAME roots — inject pattern + standard 400-step burst vs no-injection; EXACT McNEMAR one-sided p<0.01 (rev-3 named Fisher — an unpaired test — for a paired design; struck, §10-O10). Pre-registered power limit: at 30 pairs the test needs ≥7 discordant one way, so it is powered only for injection effects ≥ ~23% discordance — declared, and a smaller true effect reports as UNDERPOWERED-NULL, not absence. Scored on BOTH crossing at the root AND shadow-cell attainment (crossing-free endpoint, §10-O3).
LATTICE: T1(B−A or C−B)+T2+T3 = GATE-OPENED (requires 2 seeds). T1+T2 = enabling-not-sole. T1 alone = coverage/unattributed. None = ARM-FAILED, never NULL (NULL requires gate_armed_secs ≥ 0.5× wall-clock — rev 2 B1). PRIMARY ENDPOINT FOR THIS WEEK is instrument-grade discovery (≥1 candidate surviving NOOP-subtraction + invariance + FDR + farmability, with K1 sham null clean and shadow_yield > 0); GATE-OPENED is the stretch outcome, not the success criterion. RECLASS (boundary_state_axes 1→≥2) is a reported FINDING only, requires T2-attribution or BH-significance, can never satisfy STRATEGY G4 (rev 2 A1: G2 stays deleted).

## 3. SELF-ARMING (taxonomy-KEYED, never taxonomy-WIRED)

Pure gate_armed(mode, pin_time, now, pin_secs, typed, band_growth_ok) mirroring ortho_armed (:1224); unit-testable without a Solver. Rev-3's inputs did not exist (§10-O9): the derived pin_secs had no data source (_pin_time is overwritten in place at :1552/:1606 — verified, no interval history exists — and a saturated wall can log ZERO advances in-session, leaving the median undefined), and "TYPED in the corpus" had no runtime surface (the corpus is markdown). ARM iff ALL: (1) mode≠off; (2) now−_pin_time ≥ pin_secs where pin_secs = --gate-pin-secs, EXPLICIT and REQUIRED when mode≠off (mined defaults documented: 600 CV / 120 BB; negative disables; Contra's hand-set 120 s precedent rejected). v2 MAY derive it (8× median inter-advance interval, clamp [60,600]) only after an advance-timestamp ring buffer is appended at :1552/:1606 — deferred, not in v1; (3) --gate-target-typed passed (operator attestation, echoed in header; Contra never gets the flag); (4) band growth <5% of own peak for ≥3 checkpoints — bookkeeping SPECIFIED: band_cells (count of archive keys with key[-1] ≥ topgx−gate_band, single pass at the 60 s progress cadence) is a new progress-line field; the conjunct reads the last 3 records. NO flat_windows conjunct (rev 2 B1: stall_flat_windows==0 in 150/150 progress rows across four banked runs — it never arms). Dispatch: boundary_axis_profile(cells, band=24, bookkeeping=(4,5)).interaction_blind True → enumerate; False → search (needs sidecar). Frontier advance resets _pin_time (:1552/:1606) → self-disarm; also disarm on candidate admission, budget spend, or <1 attributed event/10k band steps for 20 min; re-arm only after a fresh pin. gated_wall_verdict() stays operator-read between sessions; only pure boundary_axis_profile runs live, on the flush snapshot (:2902 daemon), never in-loop.

## 4. TARGET ATTACK PLANS (instrument-risk order)

4a. K0 INSTRUMENT CONTROL — FIRST, BLOCKING (D2). Protocol per §0 repair 2 (rebuilt): TUNE on the Contra rediscovery probe only (hp 0x04BF–C2 / ident 0x058F–92 on contact; ≤2 revisions, Contra answers only; requires the Contra ROM-hash defect resolved first — D1 receipts batch); FREEZE; then grade BLIND, once each: CV lvl_02 stair root ($0004==8) and BB 99-0 pickup root ($0030), PASS = both at rank ≤5. Post-freeze miss on either → instrument broken, NO null from it is publishable, campaign disarms (pb-possession's sole receipt was a config comment — never again). Same session: sham-root null (K1 baseline) AND the T2 recurrence pilot (≥50 policy resamples off the CV stair-mode onset → p0, feeds §2 T2 power calc).
4b. CV HALL — PRIMARY. GATED-candidate + interaction_blind (1 live axis) → enumerate. RESUME ARCHIVE (§10-O2 — rev 3 cited only ortho_a/_ctrl and cv_chain_hw2/lvl_03_trace, omitting runs/cv_chain_hw/lvl_03_overnight: 560,410 cells / best 767 / 45.9M steps / 5.98 h / 11.9 GB / 0 solutions — 3.8× the largest cited archive, holding the wall's deepest band roots): D3 FIRST ACTION = lineage check on lvl_03_overnight (hw-flag header vs current defaults, trace tuple arity, one state-blob load_worker_state round-trip in the test suite). Compatible → it is THE resume archive for ALL T1 arms and the band-95 root count is RE-FROZEN against it (not against ortho_ctrl). Incompatible → record the exact reason HERE (hw-flag lineage / key arity / truncation) and fall back to cv_chain_hw2/lvl_03_trace. Either way §6 budgets against the true base rate: 5 runs / ~10.7 h / ~77M steps / 0 crossings. Roots: band-95 cells re-frozen per above + band cells to reach 16 + 4 sham. Falsification bar: the hand result had attack at NOOP baseline and entity_slots tracking scroll (locomotion-coupled, kill-reading refuted 2026-08-10) — the enumerator must REPRODUCE that structure or it over-calls. Then rank → sidecar → T1 three arms 90×3 (§2; --burst 64 first per rev 2 A5 prereg, --want-solutions 4, buckets binding on resume) → T2/T3. Exit-shape note carried: a CV block exit is a level_key(0x0028) change at arbitrary gx — 0x0028 telemetry stays wired so G1c can read out.
4c. BB 99-1 — KEY_BLIND repair, after K0 passes. Roots: r99_1_boss_retry band cells + entrance_after_99-0.state; 66-pattern basis; controller-2 EXCLUDED (2P join refuted — saves 2×); pass-B mandatory before classing (slow reverts). $0462 = T3 detector only, never a candidate, never a room_advance macro (F's verified no-op). Likely honest outcome = a key repair (the axis IS the missing key bit), not a GATED crack — pre-registered as such.
4d. CONTRA — DECLINE TO ARM; campaign is DECLARED CASTLEVANIA-ONLY for GATED-class claims (§10-O3, the second branch of the objection's either/or — the arm-Contra branch is REFUTED: scoring Contra GATED off concentration 43.6 (52,539/1,205) would repeat the exact error §10-O1 identifies, and FOOTPRINT_FLOOR=7,427's own docstring calls it the ELIMINATE PRECONDITION — "failing to EXCEED a prior you did not REPRODUCE is not evidence the prior is immovable... this is not an elimination" — i.e. this run's null is uninformative, not a wall verdict). BB stays instrument-control; Contra stays under-covered. Contra's role this week = the K0 TUNING CASE (§4a — promoted from side-check; the ~15-min probe budget moves there); the unspent repair re-run budget goes to coverage next week. No GATED vocabulary for Contra until its own family bands exist.

## 5. WEEK SCHEDULE (attended blocks only; NO overnight compute — binding; solver runs strictly SEQUENTIAL, loadavg in every header — risk R5)

DESCOPED (§10-O4 — rev 3 stacked ~19.5 h of strictly-sequential attended machine time (8.5 gate-opener + 4.5 Tetris + 2 Zelda + 3 B5 + 1.5 detector) on a chained plan where one slip deleted D5's only replication): Tetris sessions 1–3, Zelda calibration+verification, and ONBOARD S1–S8 are CUT from this week (~7.5 h; none feeds G1; their specs and gates carry to next week unchanged). B5 SLIPS to next week with its prereg intact (STRATEGY G2 commitment unchanged — slipped, not cut; runs as next week's first block). Detector-v2 stays (1.5 h, D2, Day-30 prerequisite, does not compete with any G1 block). DECLARED UP FRONT: if D5 is lost to slip, the week yields a SINGLE-SEED result and cannot close G1.
D1 (Mon): implementation + 28 tests + suite gates green; chain flag forwarding; receipts-hygiene batch (5 findings, 2026-08-10 audit): CLAIMS.md↔gated_wall_calibration cross-ref + conditional-positive-class caveat, Contra ROM hash re-derived + follow-up receipt (open defect — resolve BEFORE the K0 tuning probe), onboarding-receipt registry appended, three orchestrator-appended verdicts labeled.
D2 (Tue): K-FALSIFIER (offline, blocking for GATED vocabulary — §7): score the four effort-matched RESOLVED archives with solutions/progressing branches bypassed; then K0 instrument control (§4a: Contra tuning → freeze → CV/BB blind grades) + sham null + T2 recurrence pilot (p0); detector-v2 slice (combat-blip + room-transition vetoes — the two open confluence cases) + attended four-game re-run, success = zero fabricated clears (G-DETECTOR).
D3 (Wed): lvl_03_overnight lineage check (§4b) + CV discovery sweep + pass-B + rank (~40 min, §6) → sidecar; CV T1 arms A + B (90+90, sequential).
D4 (Thu): CV T1 arm C (90 min); T2 per revised spec + T3 (~45 min); BB discovery + rank (~20 min) iff on schedule. Decisive day for G1 seed 1.
D5 (Fri): PROTECTED — seed 2 of the decisive T1 arm pair (~3 h; scheduled regardless of whether seed 1 crossed, since the crossing-free endpoints also need replication); FORGE#2 verdict paragraphs per-wall, never merged; negatives in BB-possession voice. Nothing else lands D5.
D1-CONTINGENCY (any day DR-v15 D1 returns): triage per the pre-registered protocol — diff every our-system number vs dossier Part10, unit-shift=quarantine, spot-check ~3 citations/Q, classify Q1–8 before adopting; responses pre-set (backward-curriculum top candidate iff greedy-targeted + Wilson/SPRT; queryable-expert DAgger rejected; capacity step LOW; Q3 diagnostic first; Q8 pause accepted). B6 requires its own B5_PREREG-style doc.
LEAGUE: lane DIED this round — record as dead; prerequisites unchanged (detector-v2 = the D2 slice, auto-recovery has zero code); no league work this week beyond G-DETECTOR.

## 6. BUDGET (corrected — the ×8 repair; all rates AGGREGATE 8-worker: CV 1,661 / Contra 1,779 / BB 1,805 steps/s; plan at 1,700)

Uniform program 153 steps (max settle 21 + ≤108 pattern + 24 tail, padded — §10-O8; +9% on every sweep line). Pass-A = patterns × 2 phases: CV 240 programs/root (basis 120) ≈ 36.7k steps ≈ 22 s + 25% save/restore ≈ 27 s/root → 16 roots × 2 repeats ≈ 15 min; +4 sham roots ≈ +7 min; BB (144 programs/root, basis 72) ≈ 8+4 min. Pass-B: ≤16 survivors × 4 phases × ~663 steps × 3 roots ≈ 127k ≈ 2 min. Rank: CPU-only, <1 min. CV discovery total ~28 min (+ lineage check ~10 min); BB ~17 min. T1 = THREE arms × 90 min for CV (4.5 h, was 3 h). T2: pilot-sized K (or deterministic ablation) ≈ ≤15 min. T3 30 roots × 2 × 400 = 24k ≈ 5 min with restores. Totals: K0 ≈ 0.75 h (incl. Contra tuning probe + p0 pilot), K-FALSIFIER ≈ 0.5 h offline, CV ≈ 5.6 h, BB ≈ 0.4 h (discovery only; T1 conditional next week), seed-2 D5 ≈ 3 h → ≈ 10.5 h gate-opener wall across D2–D5, sequential. Support: detector re-run ≈ 1.5 h ONLY (Tetris/Zelda/B5/ONBOARD descoped, §5). Disk ≤10 GB (869 GiB free). RAM: 2 MB hist + ~3 MB/root, discarded after rank. Sweep cost is accounted as SEARCH-STEP DISPLACEMENT, not sps (§10-O3: sweep steps still increment steps_done, so sps is blind to the tax — K2 deleted, K2b 0.95× is the guard); gate_sweep_steps is its own telemetry counter so displacement is measured, not inferred.

## 7. PRE-REGISTERED GATES + KILL CRITERIA (everything)

K-FALSIFIER (NEW, D2, offline, BLOCKING for vocabulary — §10-O1): score runs/live_show/smb_4_4_micro/lvl_3-3, lvl_1-3, runs/ge_chain_w8/lvl_00_8-1, runs/lost_levels/ll_1_1_transfer (effort-matched RESOLVED archives, all on disk) through wall_taxonomy with the solutions/progressing branches bypassed. PASS = some statistic ranks the CV hall ABOVE all four (concentration alone does not: red-team scoring found 270.03/240.19/149.63/98.30 vs the hall's 136.34 — to be reproduced as this gate's first output). FAIL = strike GATED / "saturated" / any visits-as-saturation phrasing from this doc, the HUD, and FORGE#2; relabel CV UNRESOLVED-CONCENTRATED; the sweep proceeds unchanged as a pure discovery instrument (its value never depended on the label).
GATES: G1 WALL MOVES (any of, replay-verified from root, suppression-tested): G1a cell at raw progress > pin; G1b banked solution accepted by counterfactual_probe; G1c (CV) a 0x0028 value absent from every banked archive — FAIL only after all three checked AND solutions/ + exit code inspected (risk R2). G1-DISCOVERY (this week's primary, §2): ≥1 surviving candidate + clean K1 sham null + shadow_yield > 0, replicated on seed 2. G-DETECTOR (D2): four-game re-run, zero fabricated clears. DEFERRED to next week with specs intact (§5): G-B5 (prereg unchanged, pooled cold-greedy ≥0.63, flagship 0.65 protected), G-TETRIS (non-blocking, Day-60 lane; TOTALITY bar unchanged), G-ZELDA (cap-calibration protocol unchanged), G-ONBOARD (SMB recall 3/3 + Contra/Kirby pass).
KILLS: K0 instrument control fails (post-freeze miss on either blind grade) → campaign disarms, nothing publishable (2 revisions max, Contra-only tuning, CV + BB both blind — §4a). K1 SHAM NULL: sham-root yield ≥50% of wall-root yield → ranking is noise, kill. K2 DELETED (§10-O3: sweeps consume the step budget, and sps counts sweep steps as steps — the statistic is structurally blind to the tax and can never fire; displacement is measured directly via the gate_sweep_steps counter, §6). K2b SYMMETRIC COVERAGE TAX, TIGHTENED to 0.95× (rev 2 A3's 0.85 superseded — sweep-frac 0.10 lands ~0.90× control new_cells, inside the old blind spot; ortho_a's own −31% result, 38,777 v 56,369, still fires it): armed new_cells <0.95× control at equal wall-clock + fixed sig schema (arm C vs arm B only — same sidecar) + 0 crossings + 0 BH-significant lift → shelve, no silent iteration. K3 DISCOVERY NULL, RE-SCOPED per-target (§10-O3: the old "≥2 targets" conjunct was unsatisfiable — only CV can arm this week): 0 CV candidates survive NOOP-subtraction + 3-root invariance + FDR + farmability → ARM-FAILED for CV, publish as single-target; the class-level verdict waits for a second armed target (BB next week at the earliest). K4 CARDINALITY/FARMABLE: >8 values or >1 event/1k random steps → refuse the AXIS, not the arm. K5 BLOW-UP: an admitted axis grows band cells >3× with no advance in 30 min → revert it. K6 TWO SEEDS fail T1 on CV → arm eliminated for CV; class-level elimination waits for the same result on a second armed target (per-target scoping, §10-O3). K7 no axis ships without receipt-sha + provenance audit. SUPPORT KILLS (carry to next week with their lanes — §5 descope): Tetris mismatched --gx-bucket across sessions corrupts resumed max_gx → abort session; Zelda cap saturates at 255-calibrated value+2 → raise once to +8, else re-taxonomize; D1-contingency recs importing SMB knowledge → rejected on purity; onboard fix breaking Contra/Kirby → revert.

## 8. FORGE ENTRY + PURITY

FORGE#2 (gate-opener) enters CLAIMS.md in the standard two-paragraph pattern (forged+reviewed now; dated verdict AFTER the A/B pairs); scored per-wall (CV/BB/Contra), never merged; learn-vocabulary BANNED for FORGE-class (approved verbs: diagnosed/forged/built/extended itself). VALIDATION REQUIREMENTS: (i) K0 passed and cited with revision count + tuner/grader separation statement + the game it was tuned on (Contra) named in every citation; (ii) every admitted axis carries docs/receipts/probes/gate_axes_<target>.json + rank ladder + farmability verdicts + inertness on banked clears (0 mismatches); (iii) A/B header completeness (commit/seed/workers/flags/root-sha/argv/burst/ACTIVE-predicate/sidecar-sha, stdout teed, loadavg); (iv) crossing claims carry all three lattice verdicts; (v) narrative: entity_slots edges = "unconfirmed occupancy candidates" until receipted; reclass = its own non-scoring FINDING card; "coverage opened it, not the mechanism" mandatory when the falsifier fires; nulls flat, ARM-FAILED as ARM-FAILED; (vi) the K-FALSIFIER verdict is cited wherever GATED appears — if it FAILED, GATED vocabulary is absent everywhere and CV reads UNRESOLVED-CONCENTRATED; HUD says GATED (or its replacement label) only for CV, Contra stays UNTYPED, BB stays instrument-control.
PURITY (leak points enumerated, closed): addresses — full 2KB swept blind, no allowlist; actions — compositions of the DECLARED action_space only; roots — archive cells at the solver's own pin; nuisance filter — paired NOOP measured, not assumed; prior — the run's own boundary histogram; verification — our replay_verify/counterfactual/suppression/injection; naming — receipt schema has NO semantic-label field, axes are addr_0xXXXX + measured properties, lint rejects anything else; known bytes ($0004, $0030, Contra hp/ident) appear ONLY as K0 grading truth behind the tuner/grader wall; sidecar entries without a probe-receipt sha refused at merge.

## 9. RISKS (register → mitigation)

R1 comparisons flood (~786k tests/sweep) → NOOP subtraction + 3-root invariance + persistence class + farmability + BH-FDR + sham-root null (K1 is the key control). R2 gate inexpressible in the basis (entity-phase-locked or long ordered sequences) → phase replicates + phase-dependent lift is itself a finding + SEQ2 round on top-lift singles next week; declared limitation, not silently absorbed. R3 state-change ≠ opened gate ($0030 was found and the wall stood; r68 fell to an x-sig KEY) → BB pre-registered as key-repair-shaped; CV-only pass is weak — say so in FORGE#2. R4 instrument-is-the-bug (9 incidents on record) → K0 first, ≤2 revisions, fixture test per counter. R5 contention voids A/B → sequential only, loadavg in header, sps compared only at equal contention. R6 flag never arrives → chain forwards + echoes all --gate-*; abort if gate_armed_secs==0 at pin_secs + 5 min (rev-3's flat 10-min abort would have fired DURING a by-design 600 s CV pin — §10-O9). R7 provenance hole → profile+sidecar sha in header (castlevania.yaml has uncommitted deltas). R8 circular grading → tuner/grader wall (§0), reclass non-scoring, no GATED for Contra, publish the coverage falsifier. R9 A/B incomparable across sig changes → same sidecar/burst/bucket both arms; score crossings+solutions, not cells; flush-copy before reads. R10 fabricated crossing (DD's 88-action precedent) → full lattice before any FAIL/PASS; check solutions/ + exit code.

## 10. OBJECTIONS CONSIDERED (final red-team round, 2026-08-11 — ten objections, each verified against source/disk before disposition)

O1 (RT1: concentration is a clock, not a wall property; solved archives score 4–11× the GATED floor and escape only via earlier branches; "GATED currently means unsolved and left running"). VERIFIED: gated_wall_verdict tests RESOLVED (solutions>0) and PROGRESSING at branches 2–3, concentration at 7/9 — a solved archive never meets the statistic, so the calibration corpus's separating band (20.58 vs 31.04, the module's own "THINNEST shipped margin" note) never included the high-concentration solved runs; the hall's score is monotone in runtime (31→136 at flat distinct_spatial). DISPOSITION: ACCEPT the falsifier, REFUTE the kill. K-FALSIFIER added (§7, D2, blocking for vocabulary) on the four named on-disk archives (all verified present) with the escape branches bypassed; FAIL strikes GATED/"saturated"/"tried ~135x" from doc + HUD + FORGE#2 and relabels CV UNRESOLVED-CONCENTRATED. The arm is NOT killed: the interaction sweep is a discovery instrument whose value never depended on the label (§0 thesis reworded now, not conditionally).
O2 (RT1: runs/cv_chain_hw/lvl_03_overnight — 560,410 cells, 3.8× the largest cited archive — omitted from doc and corpus; real pin history 5 runs/10.7 h/77M steps/0 crossings). VERIFIED on disk: archive.stats.json cells=560,410, best_score=767, records=45.6M, solutions=0; progress tail 21,545 s / 45.9M steps; 11.9 GB. DISPOSITION: ACCEPT in full. §4b now lineage-checks it FIRST and makes it the resume archive for all T1 arms (fallback recorded in-section if incompatible); band-95 roots re-frozen against the chosen archive; header and §6 restate the 5-run/10.7 h/0-crossing base rate.
O3 (RT1: every lattice tier conditions on a crossing that never occurred; K3's ≥2-target conjunct unsatisfiable; K2 structurally cannot fire; sweep-frac 0.10 sits in K2b's blind spot; arm Contra or declare CV-only). VERIFIED: sps counts sweep steps (steps_done += workers per pool step), so the tax appears as search-step displacement, invisible to K2; only CV can arm. DISPOSITION: ACCEPT with the CV-ONLY branch. Crossing-free endpoints added as this week's PRIMARY (shadow_yield, BH-significant candidates, shadow-cell attainment; §2, G1-DISCOVERY); K2 deleted, K2b tightened to 0.95×, displacement measured via gate_sweep_steps; K3 re-scoped per-target. Arm-Contra branch REFUTED: FOOTPRINT_FLOOR's docstring (verified verbatim: "ELIMINATE PRECONDITION... not an elimination") says the Contra null is uninformative, and scoring it GATED off conc 43.6 would repeat O1's error. Campaign declared Castlevania-only for GATED-class claims (§4d).
O4 (RT1: ~19.5 h attended sequential machine time + an overloaded D1; one slip deletes the only replication; cut Tetris/Zelda/ONBOARD/B5). VERIFIED by the doc's own §6 arithmetic. DISPOSITION: ACCEPT with two modifications. Tetris/Zelda/ONBOARD cut from the week (specs+gates carried intact); B5 SLIPS with prereg unchanged rather than being cut (STRATEGY G2 is a standing commitment — deferral, not deletion); detector-v2 kept (1.5 h, Day-30 prerequisite, no G1 contention). D5 is protected for seed 2 and scheduled unconditionally (crossing-free endpoints need replication too); the single-seed-if-slipped limitation is declared up front (§5).
O5 (RT1: K0's tuner/grader wall separates by answer, not target — two CV-tuned revisions bias the ranker toward CV-shaped bytes; the BB grade re-tests the method on the case the manual form already solved). DISPOSITION: ACCEPT in full. Tuning moved to Contra (third game, receipted bytes, promoted from §4d side-check to sole tuning case); CV and BB both held blind, graded once each post-freeze; revision count + tuning target logged in the sidecar header; every K0-PASS citation names the tuning game (§0 repair 2, §4a, §8-i).
O6 (RT2: T1 tests nothing it is named for — the sidecar treatment sits in both arms, the armed arm alone pays the sweep tax on the deciding metric, and 2 of 4 --gate-opener values are unspecified). VERIFIED against rev-3 §1/§2 text. DISPOSITION: ACCEPT in full. Three-arm design A/B/C with B−A = key lift and C−B = injection lift, scored on crossings/solutions only (supersedes rev-2 B2, whose cell-comparability rationale is moot under crossing scoring); flag cut to {off,enumerate}; 2 seeds per arm pre-registered for any GATE-OPENED claim (§1, §2).
O7 (RT2: the sweep corrupts search state and its byte-identity test is unsatisfiable by construction — _assign calls select()/load_worker_state and draws from self.rng; in-flight steps and macros discarded; barren mis-debited). VERIFIED at :2555–2589 (barren debit, select(), rng draw for 'prev'). DISPOSITION: ACCEPT in full. Sweep fires at burst boundaries only; save/restore worker blob + deepcopy'd ctx verbatim; never _assign, never barren; dedicated sweep Generator; 3-part isolation test (ctx equality, savestate equality, search-rng state unchanged) (§1).
O8 (RT2: interaction_basis cannot be built to its asserted sizes — ladder gives 120/72/90, not 128/66/88; CONTACT is state-dependent and cannot be pure; program length is 153, not 140). VERIFIED by arithmetic on the declared ladder. DISPOSITION: ACCEPT in full. Generation table published as fixture data with per-target counts asserted by test; CONTACT moved to sweep-time admission with declared self-measured parameters (gx/y telemetry, eps=0, K=8); uniform length 153; §6 recomputed (+9%, T1 now 3×90) (§1, §6).
O9 (RT2: gate_armed's inputs do not exist — no advance-timestamp history behind the derived pin_secs (median undefined at zero advances), no runtime surface for "TYPED in the corpus", no band-growth bookkeeping, R6's 10-min abort fires during a legitimate 600 s pin). VERIFIED: _pin_time overwritten in place at :1552/:1606, no interval record anywhere. DISPOSITION: ACCEPT in full. v1 requires explicit --gate-pin-secs (mined defaults 600/120, negative disables); derivation deferred to v2 behind an advance-timestamp ring at those two sites; --gate-target-typed flag replaces the corpus conjunct; band_cells progress-line field specified; R6 abort moved to pin_secs + 5 min (§1, §3, §9).
O10 (RT2: T2 at K=16 exact-McNemar p<0.01 needs >44% recurrence and can never pass, routing real mechanisms to "coverage/unattributed"; T3 uses Fisher — unpaired — on a paired design and needs ≥7/30). VERIFIED by the binomial arithmetic. DISPOSITION: ACCEPT in full. K=16 killed; p0 measured in K0 (≥50 resamples off the CV stair-mode onset — a boundary that DOES open, so p0 is measurable); K set by power calc for MDE 0.3; p0<0.15 → deterministic NOOP-masked ablation reported as necessity-only; T3 switched to exact McNemar with the ≥~23%-discordance power limit pre-registered and UNDERPOWERED-NULL as an explicit outcome (§2, §4a).

---

## §11 — As-built amendments (2026-08-11, D1 implementation + repair receipts)

The shipped code diverges from §1 as follows (each with its repair
receipt in the D1 workflow journals):
1. COMBO family is rung-outer/pair-inner; cap 24 = 24 PROGRAMS; totals
   120/72/90 stand, but distinct combo MASKS are CV 24 / Contra 20 /
   BB 6 (BB has only 6 novel pairs).
2. CONTACT window: settle-only when settle supplies k+1 samples (never
   slides); extends into the pattern's first frame only when settle
   cannot; the pattern's first frame is a separate always-checked
   refusing conjunct. Phase 8 is admittable; no phase can have an
   admission manufactured from post-pattern motion.
3. BH-FDR m = the full addr x family grid computed ONCE per sweep and
   charged identically to the wall and its sham null (receipts carry
   fdr_m). §9-R1's "~786k" is superseded (4-family CV grid = 8,192).
4. gate_marks are (step, cand, kind, span) 4-tuples; 3-tuples load.
5. Telemetry adds gate_restore_failed; null sweeps write their
   candidates receipt (the pre-registered primary outcome) with empty
   admitted/ranked + full sweep params.
6. The run deadline is passed into the sweep (no overrun-by-a-sweep).
