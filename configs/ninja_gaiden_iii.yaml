name: "Ninja Gaiden III - The Ancient Ship of Doom"

rom_path: "roms/Ninja Gaiden III - The Ancient Ship of Doom (USA).nes"
rom_hashes:
  - "569b3382638a1c9094e2c40b35f359c7"
start_state_path: "roms/Ninja Gaiden III - The Ancient Ship of Doom (USA)_start.state.bin"

description: |
  Ninja Gaiden III: The Ancient Ship of Doom (NES), onboarding profile
  (wave 5, 2026-08-25). Structure copied from configs/ninja_gaiden_ii.yaml
  (NG1/NG2 adjacency); no game knowledge encoded — generic controller,
  odometer progress source pending gate.

  Start state: roms/Ninja Gaiden III - The Ancient Ship of Doom
  (USA)_start.state.bin, minted this wave
  (runs/onboard_wave5/mint_ninja_gaiden_iii.json) — liveness verified by
  input-divergence probe (RIGHT vs LEFT vs NOOP diverge 232/195/118 RAM
  bytes from a fresh reload) AND by a rendered-frame visual check
  (Stage 1-1A HUD, ninja sprite visibly displaced further right under a
  90-frame RIGHT hold vs a 90-frame NOOP hold from the same state) —
  not a title/demo screen.

frame_skip: 4

# A GENERIC controller. Tuned action sets encode another game's tactics.
action_space:
  - []
  - ["right"]
  - ["left"]
  - ["up"]
  - ["down"]
  - ["A"]
  - ["B"]
  - ["right", "A"]
  - ["right", "B"]
  - ["left", "A"]
  - ["left", "B"]
  - ["right", "A", "B"]
  - ["down", "B"]
  - ["up", "B"]

solve:
  no_clear_predicate: "no witnessed clear exists for this game, so no predicate can be validated; deliberately unminted per the 4-CONFIRMED/41-VOID roster adjudication (docs/research/CLEAR_GAP_CLOSURE_2026-08-26.md). `solutions: 0` here is a compile-time constant, never a search result."
  rom: "roms/Ninja Gaiden III - The Ancient Ship of Doom (USA).nes"
  # Odometer gate (runs/onboard_wave5/gate_ninja_gaiden_iii_right.json):
  # --forward right, 1200 steps, axis x, range 0..264, 49 distinct,
  # oam_churn 1199/1199 -- PASS, SIGNAL SOUND (tail-quarter flat noted
  # as a behaviour finding, same shape as NG1/NG2/Super C, not an
  # instrument fault). NG1/NG2 precedent: prefer the odometer as
  # progress source once it gates SOUND.
  progress: {source: odometer, axis: x}
  # y / lives / room-counter discovered by scripts/discover_observables.py's
  # own 3-probe protocol against this exact start state, 2026-08-25. No
  # RAM map, no disassembly, no walkthrough.
  # Receipt: runs/onboard_wave5/discover_ninja_gaiden_iii.json
  #   $00DC  room/screen counter (netF=264, mono=1.00, no saturation) --
  #          not used as primary progress (odometer already gated SOUND
  #          and is the preferred hardware surface), kept as `area` for
  #          archive cell-keying since it steps cleanly at transitions.
  #   $0084|$00DC<<8  spatial pair -- SATURATES (camera-clamp, caps at
  #          scroll-window limit + rebases at room transitions). Flagged
  #          by the discovery tool itself; not used.
  #   $0006  y candidate -- jump/ballistic signature.
  #   $007B  lives candidate -- decrements on death, 5 of 5 agreement.
  area: 0x00DC
  y: 0x0006
  lives: 0x007B
  clear:
    # scene_cut DECLINED 2026-08-27, superseding the 2026-08-26 arming
    # (27902e1). Re-adjudicated by measurement, not judgement:
    # scripts/scene_cut_arming.py.
    # DEMONSTRATED FALSE POSITIVE: driven at its own armed gate (scene_min=6,
    # blank_min=1, kind=['fade']) over 4x6000 steps of mixed-random play that
    # cleared nothing, this gate fired 15 times. Every one is a false positive,
    # and every one survived the death veto.
    # THE GENERAL REASON, which applies to every profile in this cohort and not
    # only this one: the audit probe CLEARS NOTHING, so its entire observed
    # (d_scene, d_blank) distribution is null. A gate at or below that null
    # fires on ordinary play; a gate above it has no evidence it can ever fire.
    # An ARM needs a WITNESSED positive -- one blank run known to be a level
    # transition -- and no profile here has one. Same refusal, same reason, as
    # docs/receipts/rygar/clear_predicate_REFUTED.md.
    # The instrument is NOT the problem: odo_blank moves for 20 of the 25
    # cohort profiles and reads 55/55 transitions on Rygar's banked tape.
    # Re-arm by witnessing one transition on this profile, registering it in
    # scene_cut_arming.WITNESSED_TRANSITIONS, and re-running the audit.
    # Reproduce: .venv/bin/python scripts/scene_cut_arming.py --profile configs/ninja_gaiden_iii.yaml
    # Receipt:   docs/receipts/clear_control/scene_cut_arming_2026-08-27.json
    signals:
      scene_cut:
        enabled: false
        reason: >-
          DECLINED 2026-08-27 by scripts/scene_cut_arming.py. DEMONSTRATED
          FALSE POSITIVE: driven at its own armed gate (scene_min=6,
          blank_min=1, kind=['fade']) over 4x6000 steps of mixed-random play
          that cleared nothing, this gate fired 15 times. Every one is a false
          positive, and every one survived the death veto. An ARM requires a
          witnessed level transition to place a length floor against; this
          profile has none.

  # DELIBERATELY EMPTY -- no clear captured yet; guessing a stage-advance
  # byte here is the authored-semantics class the purity line forbids.
  level_key: []
