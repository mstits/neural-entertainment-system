name: "Ninja Gaiden II"

rom_path: "roms/Ninja Gaiden II - The Dark Sword of Chaos (USA).nes"
rom_hashes:
  - "2b9570720145f760aef1ef04814abe92"
start_state_path: "roms/Ninja Gaiden II - The Dark Sword of Chaos (USA)_start.state.bin"

description: |
  Ninja Gaiden II: The Dark Sword of Chaos (NES), onboarding profile
  (wave 4, 2026-08-25). Structure copied from configs/ninja_gaiden.yaml
  (NG1 adjacency); no game knowledge encoded — generic controller,
  odometer progress source pending gate.

  Start state: roms/Ninja Gaiden II - The Dark Sword of Chaos
  (USA)_start.state.bin, minted this wave
  (runs/onboard_wave4/mint_ninja_gaiden_ii.json) — liveness verified by
  input-divergence probe (RIGHT vs LEFT vs NOOP diverge 492/426 RAM
  bytes from a fresh reload) AND by a rendered-frame visual check
  (Stage 1-1 HUD, ninja sprite visibly displaced further right under a
  90-frame RIGHT hold vs a 90-frame NOOP hold from the same state) —
  not a title/demo screen.

frame_skip: 4

# A GENERIC controller. Tuned action sets encode another game's tactics.
action_space:
  - []
  - ["right"]
  - ["left"]
  - ["up"]
  - ["down"]
  - ["A"]
  - ["B"]
  - ["right", "A"]
  - ["right", "B"]
  - ["left", "A"]
  - ["left", "B"]
  - ["right", "A", "B"]
  - ["down", "B"]
  - ["up", "B"]

solve:
  rom: "roms/Ninja Gaiden II - The Dark Sword of Chaos (USA).nes"
  # Odometer gate (runs/onboard_wave4/gate_ninja_gaiden_ii_right.json):
  # --forward right, 1200 steps, axis x, range 0..2043, 449 distinct,
  # oam_churn 689/1199 — PASS, SIGNAL SOUND (tail-quarter flat noted as
  # a behaviour finding, not an instrument fault). NG1 precedent
  # (configs/ninja_gaiden.yaml) also runs on the odometer, same class.
  progress: {source: odometer, axis: x}
  # y / lives discovered by scripts/discover_observables.py's own
  # 3-probe protocol against this exact start state, 2026-08-25. No RAM
  # map, no disassembly, no walkthrough.
  # Receipt: runs/onboard_wave4/discover_ninja_gaiden_ii.json
  #   $0023  progress candidate (netF=64, mono=1.00) — NOT used because
  #          the odometer already gated SOUND and is the preferred
  #          hardware surface (same reasoning as ninja_gaiden/rygar).
  #   $000E  y candidate — jump/ballistic signature.
  #   $004C  lives candidate — decrements on death, 5 of 5 agreement.
  y: 0x000E
  lives: 0x004C
  # DELIBERATELY EMPTY — no clear captured yet; guessing a stage-advance
  # byte here is the authored-semantics class the purity line forbids.
  level_key: []
