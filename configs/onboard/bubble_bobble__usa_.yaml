# DRAFT PROFILE — generated by scripts/onboard_game.py (2026-08-11).
# Not hand-verified. Read the solve: block's own comments before
# trusting any address, and re-generate rather than editing in
# place while the draft is still being calibrated.
name: Bubble Bobble (USA)
start_state_path: roms/Bubble Bobble (USA)_start.state.bin
rom_hashes:
- e6cb4e0faf2e944b2a0c8d78a399ac7f
frame_skip: 4
max_episode_steps: 2500
description: 'DRAFT onboarding profile for Bubble Bobble (USA). Every address in the solve:
  block below was drafted by the probe protocol in scripts/discover_observables.py — clean
  directional holds, an idle probe, a maneuver/death-recovery drive and a reload jump probe,
  all run on this core against this ROM. Nothing here is signed off: the calibration solve
  named under provenance.calibration has not been run yet.'
cartridge:
  mapper: 1
  nes20: false
  battery: false
  prg_kb: 128
  chr_kb: 32
provenance:
  tool: scripts/onboard_game.py
  generated: '2026-08-11'
  status: DRAFT — calibration solve not yet run; no address here has survived a search.
  protocol: scripts/discover_observables.py (find_progress / find_room_counter / find_y /
    find_hp_lives, Gate 1 flat-under-idle + Gate 2 saturation)
  evidence: our own scripted rollouts on this ROM from the start state named above
  forward: right
  start_state:
    path: roms/Bubble Bobble (USA)_start.state.bin
    source: supplied
  outside_sources_consulted: none
  calibration:
    run_by: the orchestrator, not this tool
    min_minutes_for_a_verdict: 15
    why: the wall discriminator needs 12 progress records and the solver writes one every
      60s
  probe_budget:
    clean_forward: 900
    clean_reverse: 900
    noop: 240
    advance: 1800
    settle_scan: 150
    death_drives: 3500
  receipt: runs/onboard/bubble_bobble__usa_/onboard_receipt.json

action_space:
  - []
  - ["right"]
  - ["left"]
  - ["up"]
  - ["down"]
  - ["A"]
  - ["B"]
  - ["right", "A"]
  - ["left", "A"]
  - ["right", "B"]
  - ["left", "B"]
  - ["up", "A"]
  - ["right", "up"]
  - ["down", "B"]

solve:
  rom: "roms/Bubble Bobble (USA).nes"
  # progress: <none isolated — inspect candidates in the receipt>
  y: 0x0021   # jump/ballistic signature
  level_key: []   # coverage baseline; wire a stage-advance byte here once a real clear is captured
  lives: 0x002E   # decrements on death
